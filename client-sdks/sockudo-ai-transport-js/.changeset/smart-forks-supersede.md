---
"@sockudo/ai-transport": minor
---

Add the `supersedes` transport header end to end, hide replaced suspended runs from branch
selection, and fork Vercel client tool results with a full projection seed. Export
`createToolResultFork` for callers that submit tool resolutions through a view directly.
